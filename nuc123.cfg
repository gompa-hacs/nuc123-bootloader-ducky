# Raspberry Pi Zero BCM2835 GPIO bitbang adapter
adapter driver bcm2835gpio
adapter gpio swclk 14
adapter gpio swdio 15
adapter gpio srst 18
transport select swd
adapter speed 1000

reset_config srst_only

# Housekeeping stuff for OpenOCD targeting Nuvoton NUC123 , NUC126 and others.
# by Ein Terakawa <applause@elfmimi.jp>
# based on TheLastMutt's mini51.cfg

source [find target/numicro.cfg]

#load_script mem_helper.tcl
proc mrw {reg} {
     set value ""
     mem2array value 32 $reg 1
     return $value(0)
}
add_usage_text mrw "address"
add_help_text mrw "Returns value of word in memory."

proc GetPartName {id} {
    set parts([expr 0x12305]) "NUC123SC2AN1"
    set parts([expr 0x12315]) "NUC123SD4AN0"
    set parts([expr 0x12325]) "NUC123LC2AN1"
    set parts([expr 0x12335]) "NUC123LD4AN0"
    set parts([expr 0x12345]) "NUC123ZC2AN1"
    set parts([expr 0x12355]) "NUC123ZD4AN0"
    set parts([expr 0x10012305]) "NUC123SC2AE1"
    set parts([expr 0x10012315]) "NUC123SD4AE0"
    set parts([expr 0x10012325]) "NUC123LC2AE1"
    set parts([expr 0x10012335]) "NUC123LD4AE0"
    set parts([expr 0x10012345]) "NUC123ZC2AE1"
    set parts([expr 0x10012355]) "NUC123ZD4AE0"
    if [info exists parts($id)] {
        return $parts($id)
    } else {
        return ""
    }
}

# Tcl open() does not expand '~' — use this before open
proc resolve_path {path} {
    if {[string index $path 0] eq "~"} {
        if {[string length $path] == 1} {
            return $::env(HOME)
        }
        if {[string index $path 1] eq "/"} {
            return [file join $::env(HOME) [string range $path 2 end]]
        }
    }
    return [file normalize $path]
}

proc InitandUnlock {} {
    mww 0xe000edf0 0x05f0003
    mww 0xe0002008 0x000000
    mww 0xe000200C 0x000000
    mww 0xe0002010 0x000000
    mww 0xe0002014 0x000000
    mww 0x50000100 0x59
    mww 0x50000100 0x16
    mww 0x50000100 0x88
}

proc ReadViaISP {adr} {
    mww 0x5000c000 0x39
    mww 0x5000c00c 0x00
    mww 0x5000c004 $adr 
    mww 0x5000c010 1
    while {[mrw 0x5000c010] != 0} { puts stderr "." }
    set out [mrw 0x5000c008]
    mww 0x5000c000 0x00
    return $out
}

proc WriteViaISP {adr dat} {
    mww 0x5000c000 0x79
    mww 0x5000c00c 0x21
    mww 0x5000c004 $adr  
    mww 0x5000c008 $dat  
    mww 0x5000c010 1
    while {[mrw 0x5000c010] != 0} { puts stderr "." }
    if {[mrw 0x5000c000] & 0x40} { error "ISP Error" }
    mww 0x5000c000 0x00
}

proc EnableLDROMUpdate {} {
    InitandUnlock
    mww 0x5000c000 0x79
}

proc PageErase {adr} {
    mww 0x5000c000 0x79
    mww 0x5000c00c 0x22
    mww 0x5000c004 $adr  
    mww 0x5000c010 1
    sleep 20
    while {[mrw 0x5000c010] != 0} { puts stderr "."; sleep 10 }
    if {[mrw 0x5000c000] & 0x40} { error "ISP Error" }
    mww 0x5000c000 0x00
}

proc WriteConfigRegsImpl {conf0 args} {
    InitandUnlock
    set prev_conf0 [ReadViaISP 0x300000]
    if {($prev_conf0 & 2) == 0} {
        puts [format "Config0 (0x00300000):0x%X" $prev_conf0]
        puts [format "Config1 (0x00300004):0x%X" [ReadViaISP 0x300004]]
        error "Flash is locked! Run ChipErase first."
    }
    foreach arg $args {
        if [info exists conf1] {
            error "Too many arguments"
        }
        set conf1 $arg
    }
    if {![info exists conf1]} {
        set conf1 [ReadViaISP 0x300004]
    }
    PageErase 0x300000
    WriteViaISP 0x300000 $conf0
    WriteViaISP 0x300004 $conf1
}

proc WriteConfigRegs {conf0 args} {
    init
    halt
    set id [mrw 0x50000000]
    set part [GetPartName $id]
    if {$part != ""} {
        puts [format "Device ID (PDID) :0x%08X (%s)" $id $part]
    } else {
        puts [format "Device ID (PDID) :0x%08X" $id]
    }
    WriteConfigRegsImpl $conf0 {*}$args
    puts "Configuration registers have been Written"
}

proc ReadConfigRegs {} {
    init
    puts "Reading Configuration registers."
    set id [mrw 0x50000000]
    set part [GetPartName $id]
    if {$part != ""} {
        puts [format "Device ID (PDID) :0x%08X (%s)" $id $part]
    } else {
        puts [format "Device ID (PDID) :0x%08X" $id]
    }
    halt
    InitandUnlock
    set conf0 [ReadViaISP 0x300000]
    set conf1 [ReadViaISP 0x300004]
    puts [format "Config0 (0x00300000):0x%X" $conf0]
    puts [format "Config1 (0x00300004):0x%X" $conf1]
}

proc ChipErase {} {
    init
    halt
    InitandUnlock
    set conf0 [ReadViaISP 0x300000]
    if {$conf0 & 2} {
        puts "Flash is not locked."
    } else {
        puts "Flash is locked! Erasing anyway."
    }
    mww 0x5000c000 0x79
    mww 0x5000c01c 0x01
    if {[mrw 0x5000c000] & 0x40} { error "ISP Error" }
    if {[mrw 0x5000c010] != 0} { error "ISP Busy error" }
    mww 0x5000c00c 0x26
    mww 0x5000c004 0
    puts "Performing chip erase."
    mww 0x5000c010 1
    sleep 50
    while {[mrw 0x5000c010] != 0} { puts stderr "."; sleep 10 }
    if {[mrw 0x5000c000] & 0x40} { error "ISP Error" }
    mww 0x5000c000 0x00
    mww 0x5000c01c 0
    puts "ChipErase complete."
}

proc SysReset {args} {
    init
    foreach arg $args {
        set a [string tolower $arg]
        if {$a eq "ldrom"} {
            set ldrom 1
        } elseif {$a eq "aprom"} {
            set aprom 1
        } elseif {$a eq "cpu"} {
            set cpurst 1
        } elseif {$a eq "chip" || $a eq "por"} {
            set chiprst 1
        } elseif {$a eq "run"} {
            set run 1
        } elseif {$a eq "halt"} {
            set halt 1
        } else {
            error "Unexpected argument '$arg'"
        }
    }
    halt
    InitandUnlock
    if {[info exists chiprst]} {
        mww 0x50000008 [expr {[mrw 0x50000008] | 0x01}]
    } else {
        if {[info exists aprom]} {
            mww 0x5000c000 [expr {[mrw 0x5000c000] & ~0x02}]
        }
        if {[info exists ldrom]} {
            mww 0x5000c000 [expr {[mrw 0x5000c000] | 0x02}]
        }
        if {[info exists cpurst]} {
            mww 0x50000008 [expr {[mrw 0x50000008] | 0x02}]
        } elseif {[info exists run]} {
            reset run
        } elseif {[info exists halt]} {
            reset halt
        } else {
            reset
        }
    }
}

# Write binary file to APROM using ISP commands
proc WriteAPROM {filename} {
    init
    halt
    InitandUnlock
    set filename [resolve_path $filename]

    set f [open $filename rb]
    set filesize [file size $filename]
    puts [format "File size: 0x%X bytes" $filesize]
    
    # Chip erase APROM
    puts "Erasing APROM..."
    mww 0x5000c000 0x79
    mww 0x5000c01c 0x01
    if {[mrw 0x5000c000] & 0x40} { error "ISP Error" }
    mww 0x5000c00c 0x26
    mww 0x5000c004 0
    puts "Performing chip erase."
    mww 0x5000c010 1
    sleep 50
    while {[mrw 0x5000c010] != 0} { puts stderr "."; sleep 10 }
    if {[mrw 0x5000c000] & 0x40} { error "ISP Error" }
    mww 0x5000c000 0x00
    mww 0x5000c01c 0
    puts "APROM erase complete."
    
    # Write 4 bytes at a time
    for {set adr 0} {$adr < $filesize} {incr adr 4} {
        set data [read $f 4]
        if {[string length $data] < 4} {
            append data [string repeat "\xff" [expr {4 - [string length $data]}]]
        }
        if {[binary scan $data Iu word] != 1} {
            error "APROM read short at 0x[format %X $adr]"
        }
        WriteViaISP [expr {0x00000000 + $adr}] $word
        if {$adr % 256 == 0} { puts [format "Written 0x%X bytes..." $adr] }
    }
    close $f
    puts "APROM write complete."
}

proc CheckLDROM {{mode strict}} {
    set words [read_memory 0x00100000 32 2]
    set sp [lindex $words 0]
    set rv [lindex $words 1]
    puts [format "LDROM @0x00100000 (bus): SP=0x%08X Reset=0x%08X" $sp $rv]
    if {$sp == 0xFFFFFFFF && $rv == 0xFFFFFFFF} {
        set isp_sp [mrw 0x00100000]
        set isp_rv [mrw 0x00100004]
        puts [format "LDROM @0x00100000 (ISP):  SP=0x%08X Reset=0x%08X" $isp_sp $isp_rv]
    }
    if {$mode eq "known-good"} {
        if {$sp != 0x20000400} {
            error "Known-good image expects SP=0x20000400"
        }
    } else {
        if {$sp == 0x20000400} {
            error "LDROM SP=0x20000400 — flash *-recovery.bin (SP=0x20000800), not known-good"
        }
        if {$sp != 0x20000800} {
            error "LDROM SP must be 0x20000800 (got 0x[format %08X $sp])"
        }
    }
    if {($rv & 0xFFF00000) != 0x00100000} {
        error "LDROM reset vector not in LDROM"
    }
    set tag [lindex [read_memory 0x00100D88 32 1] 0]
    puts [format "Build tag @0x00100D88: 0x%08X (recovery=0x466FC3D0, known-good=none)" $tag]
}

proc ProgramLDROMBin {filename {mode strict}} {
    init
    halt
    EnableLDROMUpdate
    set filename [resolve_path $filename]
    set sz [file size $filename]
    if {$mode eq "strict" && $sz == 3692} {
        error "Refuse 3692-byte bin (stale). Use make recovery -> *-recovery.bin (3576 bytes)"
    }
    puts "Programming $filename ($sz bytes) at 0x00100000 ..."
    program $filename 0x00100000 verify
    verify_image $filename 0x00100000 bin
    puts "Programming finished."
    CheckLDROM $mode
    puts "Starting LDROM (CPU must run for USB — do not leave target halted)..."
    SysReset ldrom run
    puts "Unplug SWD, wait 5s, plug USB only, then: lsusb -d 0416:bdf0"
}

# Write binary file to LDROM using ISP commands
proc WriteLDROM {filename} {
    init
    halt
    EnableLDROMUpdate
    set filename [resolve_path $filename]

    set f [open $filename rb]
    set filesize [file size $filename]
    puts [format "File size: 0x%X bytes" $filesize]
    
    # Erase LDROM page (4KB)
    puts "Erasing LDROM..."
    PageErase 0x00100000
    puts "LDROM erase complete."
    
    # Write 4 bytes at a time
    for {set adr 0} {$adr < $filesize} {incr adr 4} {
        set data [read $f 4]
        if {[string length $data] < 4} {
            append data [string repeat "\xff" [expr {4 - [string length $data]}]]
        }
        if {[binary scan $data Iu word] != 1} {
            error "LDROM read short at 0x[format %X $adr]"
        }
        WriteViaISP [expr {0x00100000 + $adr}] $word
        if {$adr % 256 == 0} { puts [format "Written 0x%X bytes..." $adr] }
    }
    close $f
    puts "LDROM write complete."
    CheckLDROM
}

if 0 { }